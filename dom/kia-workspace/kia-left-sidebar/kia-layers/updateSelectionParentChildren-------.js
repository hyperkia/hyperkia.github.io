function Index(){
	// const newObj = KIA.state.ui.dirtyMap.newChild;
	// const parentKey = newObj.parent;
	// const parentEl = KIA.kiaLayers._qs(`[data-item="${parentKey}"]`);
	// if(!parentEl) return;
	// const parentChildsEl = parentEl.querySelector('.childs');
	// const parentObj = KIA.state.layers.map[parentKey] || KIA.state.pages.map[parentKey];

	// parentEl.dataset.children = parentObj.children.length+1;

	// if(parentEl.open) {
	// 	const itemTemplate = KIA.kiaLayers.$id.layerItemTemplate.content.cloneNode(true);
	//     const itemEl = itemTemplate.querySelector('.parent');
	//     itemEl.dataset.item = newObj.id;
	//     itemEl.dataset.children = newObj.children.length;
	//     const layerNameEl = itemTemplate.querySelector('.parent-title');
	//     layerNameEl.textContent = newObj.nodeName;
	//     const itemVisibleEl = itemTemplate.querySelector('.item-visible');
	//     itemVisibleEl.dataset.visiblity = newObj.style.visibility;
	//     const itemLockEl = itemTemplate.querySelector('.item-lock');
	//     itemLockEl.dataset.lock = newObj.style['pointer-events'];

	// 	parentChildsEl.appendChild(itemTemplate)
	// }
}

export default Index;