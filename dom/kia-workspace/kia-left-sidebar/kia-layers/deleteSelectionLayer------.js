function Index() {
	// const deleteLayer = KIA.state.ui.getProp('dirtyMap').deleteLayer;
	// const itemEl = KIA.kiaLayers._qs(`[data-item="${deleteLayer.id}"]`);
	// const parentKey = deleteLayer.parent;
	// const parentObj = KIA.state.layers.map[parentKey] || KIA.state.pages.map[parentKey];
	// const parentEl = KIA.kiaLayers._qs(`[data-item="${parentKey}"]`);

	// itemEl?.remove();
	// parentEl.dataset.children = parentObj.children.length;
}

export default Index;